# Leave Management System

A web application that allows employees to apply for leave, managers to approve/reject requests, and HR to manage leave policies, balances, and reports.

## Features
- Employee leave application and submission functionality
- Manager leave approval and rejection workflow
- HR administration of leave policies and employee records
- Integration with existing HRMS via REST API
- User authentication and authorization

## Quick Start

### Local Development

```bash
# Create virtual environment
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt

# Run the application
uvicorn app.main:app --reload
```

Open http://localhost:8000

### Docker

```bash
docker-compose up --build
```

## API Documentation

- Swagger UI: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

## Project Structure

```
leave_management_system/
├── app/
│   ├── api/routes.py      # API endpoints
│   ├── models/models.py   # Database models
│   ├── schemas/schemas.py # Pydantic schemas
│   ├── db/database.py     # Database config
│   └── main.py            # Application entry
├── templates/index.html   # Frontend UI
├── static/                # Static files
├── requirements.txt
├── Dockerfile
└── docker-compose.yml
```

---
*Generated by Smart SDLC AI Platform*
